<div class="pull-right" style="margin-top: 20px">
  <%= link_to("新建职缺", new_admin_job_path, class: %w(btn btn-success), data: { toggle: "modal", target: "#newJobModal"  }) %>
</div>

<!-- Modal -->
<div class="modal fade" id="newJobModal" tabindex="-1" role="dialog" aria-labelledby="newJobModalLabel">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <!-- <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="newJobModalLabel">Modal title</h4>
      </div> -->
      <!-- <div class="modal-body">
        ...
      </div> -->
      <!-- <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Save changes</button>
      </div> -->
    </div>
  </div>
</div>

<table class="table table-striped table-responsive">
  <thead>
    <tr>
      <td>职缺名称</td>
      <td>类别</td>
      <td>收到履历数</td>
      <td>薪资范围</td>
      <td>管理选项</td>
      <td>发布时间</td>
    </tr>
  </thead>
  <tbody>
    <% @jobs.each do |job| %>
      <tr>
        <td>
          <%= render_job_status(job) %>
          <%= link_to(job.title, admin_job_path(job)) %>
        </td>
        <td><%= job.category %></td>
        <td><%= link_to(job.resumes.count, admin_job_resumes_path(job)) %></td>
        <td>
          <%= job.wage_lower_bound %>～
          <%= job.wage_upper_bound %>
        </td>
        <td>
          <%= link_to edit_admin_job_path(job), class: "btn btn-xs btn-info" do %><i class="fa fa-pencil"></i><% end %>
          <%= link_to admin_job_path(job), :method => :delete, :class => "btn btn-xs  btn-danger", :data => { :confirm => "真的要删除吗？" } do %><i class="fa fa-trash-o"></i><% end %>
          <% if job.is_hidden %>
            <%= link_to publish_admin_job_path(job) , :method => :post, :class => "btn btn-xs btn-success" do %><i class="fa fa-eye"></i><% end %>
          <% else %>
            <%= link_to hide_admin_job_path(job), :method => :post,  :class => "btn btn-xs btn-warning" do %><i class="fa fa-eye-slash"></i><% end %>
          <% end %>
        </td>
        <td>
          <%= job.created_at %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
